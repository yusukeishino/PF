<% @images.each do |image| %>
 <%= attachment_image_tag image, :image, size: "150x150", format: 'jpg', fallback: "no_image.jpg" %>
 <!--投稿画像-->
 <%= image.image_id %>
 <!--画像説明-->
 <%= image.body %>
<% end %>